syntax="proto3";

// To generate
//protoc --proto_path=./  *.proto --go_out=./

package mmoProto;

option go_package = "/pb";

enum ServerRequestTypeE {
    CHARACTER_UPDATE = 0;
}

message ServerRequest {
    ServerRequestTypeE type = 1;
    int32 seq_id = 2;
}